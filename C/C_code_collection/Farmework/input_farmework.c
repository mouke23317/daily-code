meun();       //初始化菜单    
choose = Simple_get_frist_char();//获取字符串中第一个字符
do{
        switch(choose)
        {
            
        }
        choose = Simple_get_frist_char();
}while((choose != 'q') && (choose != 'Q'));